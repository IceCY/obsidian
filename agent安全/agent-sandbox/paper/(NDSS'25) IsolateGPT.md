### 威胁模型
- 设想的是用户的LLM-based System中可能会集成很多来自第三方的LLM-based App
- 这些App是不可信的
- App可能从外界（不可信）数据源获取的数据也是不可信的
	- 潜在风险：污染上下文、进行prompt injection攻击
### 核心目标
* 将不可信App及其获取（不可信）数据所带来的危害限制在App范围之内
* 利用Hub作为中枢，来进行查询分配与恶意行为检查